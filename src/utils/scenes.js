export const toPage = (key,value) =>{
  var model = {};
  model[key] = value || '';
  return model;
}

export const alert = (message)=>{

}

export const api = (url, params, confirm, success, error)=>{

}

export const dialog = ()=>{
  
}
